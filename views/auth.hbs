<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Auth Test</title>
  </head>
  <body>
    <div class="header">
      <h1>Auth Test</h1>
      <p>Test.</p>
    </div>
    <p>
      <button type="button" name="gettoken">Update token</button>
      <span class="message allow">Authenticated<span class="icon">&check;</span></span>
      <span class="message deny">Not authenticated<span class="icon">&cross;</span></span>
    </p>
    <div class="login">
      <div class="section github">
        <h3>GitHub</h3>
        <button type="button" name="github" class="auth">Log in with GitHub</button>
      </div>
      <div class="section local">
        <h3>Local</h3>
        <form class="" action="index.html" method="post">
          <input type="text" name="email" value="" placeholder="Enter your email">
          <input type="password" name="password" value="" placeholder="Enter your password">
          <button type="button" name="local" class="auth">Log in with email/password</button>
        </form>
      </div>
    </div>
    <div class="section local">
      <h3>Reponse Output</h3>
      <div class="code output"></div>
    </div>
  </body>
  <style media="screen">
    body {
      max-width: 700px;
      margin: 0 auto;
      margin-top: 70px;
      font-family: sans-serif;
    }
    .header {
      margin-bottom: 30px;
    }
    .header h1 {
      font-size: 250%;
    }
    .message {
      display: none;
      margin-left: 15px;
      padding: 5px 10px 5px 8px;
      font-style: italic;
      color: white;
      border-radius: 5px;
    }
    .message .icon {
      margin-left: 10px;
      font-weight: bold;
      font-style: normal;
    }
    .allow {
      background-color: #70af26;
    }
    .deny {
      background-color: #a2320f;
    }
    .section {
      margin-bottom: 25px;
      padding: 5px 10px;
      border-left: 1px solid #ccc;
      background: #fbfbfb;
    }
    .section h3 {
      margin: 5px 0 10px 0;
    }
    .section input {
      margin-bottom: 5px;
      padding: 10px;
      width: 80%;
      font-size: 85%;
    }
    button {
      padding: 10px;
      cursor: pointer;
      margin-bottom: 5px;
    }
    .code {
      width: 90%;
      background-color: black;
      color: white;
      font-family: monospace;
      padding: 20px;
      min-height: 300px;
    }
  </style>
  <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
  <script type="text/javascript">
    window.ADAPT = {};
    $(() => {
      $('button[name=gettoken]').click(getToken);
      $('button.auth').click(authenticate);
    });
    function getToken(callback) {
      $.get('/api/auth/check')
        .done((data) => {
          window.ADAPT.tokenData = data;
          showMessage('allow');
          $('.code').text(JSON.stringify(data,null,2));
        })
        .fail((jqXhr) => {
          const error = jqXhr.responseJSON;
          if(error.statusCode === 401) {
            showMessage('deny');
            $('.login').css('display', 'initial');
            return;
          }
          $('.code').html(error);
        });
    }
    function authenticate(e) {
      e.preventDefault();
      switch($(e.currentTarget).attr('name')) {
        case 'local':
          break;
        case 'github':
          window.location = '/api/connect/github';
          break;
      }
    }
    function showMessage(name) {
      $(`.message`).css('display', 'none');
      $(`.message.${name}`).css('display', 'inline');
    }
  </script>
</html>
